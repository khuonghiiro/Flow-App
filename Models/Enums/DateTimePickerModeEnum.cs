namespace FlowMy.Models.Enums
{
    /// <summary>
    /// Enum cho các chế độ picker
    /// </summary>
    public enum DateTimePickerModeEnum
    {
        DateTime,  // Chọn cả ngày và giờ
        Date,      // Chỉ chọn ngày
        Time       // Chỉ chọn giờ
    }
}
