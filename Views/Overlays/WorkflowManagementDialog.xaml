<Window x:Class="FlowMy.Views.Overlays.WorkflowManagementDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:FlowMy.Controls"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:conv="clr-namespace:FlowMy.Converters"
        mc:Ignorable="d"
        Title="Quản lý Workflows"
        Height="500"
        Width="600"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}"
        ResizeMode="CanResize">
    <Window.Resources>
        <conv:IconKeyToPathConverter x:Key="IconKeyToPathConverter"/>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Danh sách Workflows đã lưu"
                   FontSize="18"
                   FontWeight="Bold"
                   Foreground="{DynamicResource TextBrush}"
                   Margin="0,0,0,15"/>

        <!-- List of workflows -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl x:Name="WorkflowItemsControl"
                          ItemsSource="{Binding Workflows}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource CardColor}"
                                BorderBrush="{DynamicResource BorderColor}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="12"
                                Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}"
                                               FontSize="14"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource TextBrush}"
                                               Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding FilePath}"
                                               FontSize="11"
                                               Foreground="{DynamicResource TextBrush}"
                                               Opacity="0.7"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1"
                                            Orientation="Horizontal"
                                            VerticalAlignment="Center">
                                    <Button x:Name="EditButton"
                                            Click="EditButton_Click"
                                            Tag="{Binding}"
                                            Style="{DynamicResource TransparentButtonStyle}"
                                            Padding="8,4"
                                            Margin="0,0,8,0"
                                            ToolTip="Sửa tên workflow"
                                            Cursor="Hand">
                                        <StackPanel Orientation="Horizontal">
                                            <controls:SvgViewboxEx Source="{Binding Source={x:Static sys:String.Empty}, 
                                                         Converter={StaticResource IconKeyToPathConverter}, 
                                                         ConverterParameter='edit'}"
                                                      Width="14" Height="14" Fill="{DynamicResource PrimaryBrush}" Margin="0,0,4,0"/>
                                            <TextBlock Text="Sửa" Foreground="{DynamicResource PrimaryBrush}" FontSize="12"/>
                                        </StackPanel>
                                    </Button>

                                    <Button x:Name="DeleteButton"
                                            Click="DeleteButton_Click"
                                            Tag="{Binding}"
                                            Style="{DynamicResource TransparentButtonStyle}"
                                            Padding="8,4"
                                            ToolTip="Xóa workflow"
                                            Cursor="Hand">
                                        <StackPanel Orientation="Horizontal">
                                            <controls:SvgViewboxEx Source="{Binding Source={x:Static sys:String.Empty}, 
                                                         Converter={StaticResource IconKeyToPathConverter}, 
                                                         ConverterParameter='trash-alt'}"
                                                      Width="14" Height="14" Fill="{DynamicResource DangerBrush}" Margin="0,0,4,0"/>
                                            <TextBlock Text="Xóa" Foreground="{DynamicResource DangerBrush}" FontSize="12"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Footer buttons -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button x:Name="CloseButton"
                    Click="CloseButton_Click"
                    Style="{StaticResource PrimaryButton}"
                    Padding="20,8"
                    Content="Đóng"/>
        </StackPanel>
    </Grid>
</Window>
