<local:BaseNodeDialog x:Class="FlowMy.Views.Overlays.KeyValueBridgeNodeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FlowMy.Views.Overlays"
        xmlns:converters="clr-namespace:FlowMy.Converters"
        Title="KeyValue Bridge"
        WindowStyle="None"
        ResizeMode="CanResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        Width="450"
        Height="720"
        MinWidth="350"
        MaxWidth="800"
        MinHeight="400">
    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
    </Window.Resources>
    <Border CornerRadius="12" Padding="0" Style="{DynamicResource DialogOuterBorder}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{DynamicResource DialogHeaderBorder}" Padding="16,12,12,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="TitleTextBox"
                          Grid.Column="0"
                          Text="{Binding NodeTitle, UpdateSourceTrigger=PropertyChanged}"
                          Style="{DynamicResource BaseTextBoxV2}"
                          FontSize="16"
                          Padding="0,4,0,4"
                          VerticalContentAlignment="Center"
                          Cursor="IBeam"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="PlayButton" Padding="0,0,0,0"  Width="24" Height="24" Content="?" FontSize="12"
                            Style="{DynamicResource PrimaryButton}"
                            Cursor="Hand"
                            Margin="8,0,0,0" ToolTip="Ch?y logic node n�y"
                            Command="{Binding RunSingleNodeCommand}"/>

                        <Button x:Name="CloseButton" Padding="0,0,0,0"  Width="24" Height="24"
                            Style="{DynamicResource DangerButton}"
                            Content="�"
                            FontSize="12"
                            FontWeight="Bold"
                            Cursor="Hand"
                            Margin="8,0,0,0" Click="CloseButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <TabControl Grid.Row="1"
                        Background="Transparent"
                        BorderThickness="0">
                <TabItem Header="Logic" Style="{StaticResource HttpTabItemStyle}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
                        <StackPanel>
                            <TextBlock Text="Hi?n th? ti�u d?:"
                                       Foreground="{DynamicResource TextBrush}"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,8"/>

                            <ComboBox x:Name="TitleDisplayModeComboBox"
                                      Height="36"
                                      Style="{DynamicResource BaseComboBox}"
                                      Margin="0,0,0,16"
                                      ItemsSource="{Binding TitleDisplayModeOptions}"
                                      SelectedValuePath="Value"
                                      DisplayMemberPath="DisplayName"
                                      SelectedValue="{Binding TitleDisplayMode, Mode=TwoWay}"/>

                            <TextBlock Text="M�u ti�u d?:"
                                       Foreground="{DynamicResource TextBrush}"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,8"/>

                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <ComboBox x:Name="TitleColorComboBox"
                                          Grid.Column="0"
                                          Height="36"
                                          Style="{DynamicResource BaseComboBox}"
                                          ItemsSource="{Binding TitleColorOptions}"
                                          SelectedValuePath="Key"
                                          DisplayMemberPath="DisplayName"
                                          SelectedValue="{Binding TitleColorKey, Mode=TwoWay}"
                                          SelectionChanged="TitleColorComboBox_SelectionChanged"/>

                                <Border Grid.Column="1"
                                        x:Name="TitleColorPreview"
                                        Width="36" Height="36"
                                        CornerRadius="6"
                                        Margin="8,0,0,0"
                                        BorderBrush="{DynamicResource ControlBorderBrush}"
                                        BorderThickness="1"/>
                            </Grid>

                            <StackPanel Margin="0,0,0,12">
                                <CheckBox Content="Ch? d? Pass (ghi KV t? lu?ng)"
                                          IsChecked="{Binding IsPassKeyMode, Mode=TwoWay}"
                                          Foreground="{DynamicResource TextBrush}"
                                          FontSize="14"/>
                                <TextBlock TextWrapping="Wrap"
                                           Foreground="{DynamicResource TextSecondary}"
                                           FontSize="11"
                                           LineHeight="16"
                                           Margin="22,4,0,10"
                                           Text="B?t: hi?n c?ng IN/OUT tr�n canvas � m?i l?n lu?ng ch?y qua, gi� tr? du?c append v�o kho key/value runtime (d�ng chung trong m?t l?n ch?y workflow). T?t: chuy?n sang Get � d?c KV d� c� theo interval, thu?ng kh�ng c?n n?i c?ng IN (tr? khi b?t �Xo� d? li?u� b�n du?i)."/>
                                <CheckBox Content="Xo� d? li?u (cleanup KV runtime)"
                                          IsChecked="{Binding EnableDataCleanup, Mode=TwoWay}"
                                          Foreground="{DynamicResource TextBrush}"
                                          FontSize="13"/>
                                <TextBlock TextWrapping="Wrap"
                                           Foreground="{DynamicResource TextSecondary}"
                                           FontSize="11"
                                           LineHeight="16"
                                           Margin="22,4,0,0"
                                           Text="B?t: node d�ng d? x�a KV theo c?u h�nh m?c �KV Data Cleanup�. C?ng IN tr�n canvas lu�n hi?n d? b?n n?i lu?ng k�ch ho?t cleanup � k? c? khi Pass dang t?t (Get)."/>
                            </StackPanel>

                            <StackPanel Visibility="{Binding EnableDataCleanup, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                            <StackPanel Visibility="{Binding IsPassKeyMode, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock Text="Key Identifier"
                                           Foreground="{DynamicResource TextBrush}"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,8"/>
                                <TextBox Text="{Binding KvChannelKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{DynamicResource BaseTextBoxV2}"
                                         Height="36"
                                         Margin="0,0,0,16"/>
                            </StackPanel>

                            <StackPanel Visibility="{Binding IsPassKeyMode, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                <TextBlock Text="Source node (Pass Key bridge)"
                                           Foreground="{DynamicResource TextBrush}"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,8"/>
                                <ComboBox x:Name="SourceBridgeCombo"
                                          Height="36"
                                          Style="{DynamicResource BaseComboBox}"
                                          Margin="0,0,0,12"
                                          ItemsSource="{Binding PassBridgeSourceOptions}"
                                          SelectedValuePath="NodeId"
                                          DisplayMemberPath="Title"
                                          SelectedValue="{Binding SelectedSourceBridgeNodeId, Mode=TwoWay}"/>

                                <TextBlock Text="Key"
                                           Foreground="{DynamicResource TextBrush}"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,8"/>
                                <ComboBox x:Name="KeyCombo"
                                          Height="36"
                                          Style="{DynamicResource BaseComboBox}"
                                          Margin="0,0,0,12"
                                          IsEditable="False"
                                          ItemsSource="{Binding KnownKeyOptions}"
                                          SelectedItem="{Binding KvChannelKey, Mode=TwoWay}"/>

                                <TextBlock Text="Fetch interval"
                                           Foreground="{DynamicResource TextBrush}"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,8"/>
                                <Grid Margin="0,0,0,16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0"
                                             Margin="0,0,8,0"
                                             Style="{DynamicResource BaseTextBoxV2}"
                                             Height="36"
                                             Text="{Binding PollIntervalValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                    <ComboBox Grid.Column="1"
                                              Height="36"
                                              Style="{DynamicResource BaseComboBox}"
                                              ItemsSource="{Binding PollUnitOptions}"
                                              SelectedValuePath="Unit"
                                              DisplayMemberPath="Label"
                                              SelectedValue="{Binding PollIntervalUnit, Mode=TwoWay}"/>
                                </Grid>
                            </StackPanel>

                            <StackPanel Visibility="{Binding IsPassKeyMode, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock Text="Input mapping (Pass mode):"
                                           Foreground="{DynamicResource TextBrush}"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,8"/>
                                <Border Background="{DynamicResource WindowBodyBackground}"
                                        BorderBrush="{DynamicResource ControlBorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="12"
                                        Margin="0,0,0,16">
                                    <StackPanel x:Name="InputsPanel"/>
                                </Border>

                                <TextBlock Text="Value append sources (multiple)"
                                           Foreground="{DynamicResource TextBrush}"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,8"/>
                                <ItemsControl ItemsSource="{Binding AdditionalAppendSources}" Margin="0,0,0,8">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="{DynamicResource HeaderBackgroundBrush}" BorderBrush="{DynamicResource ControlBorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,0,0,8">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <ComboBox Grid.Column="0"
                                                              Height="32"
                                                              Margin="0,0,8,0"
                                                              Style="{DynamicResource BaseComboBox}"
                                                              ItemsSource="{Binding AvailableSources}"
                                                              SelectedValuePath="NodeId"
                                                              DisplayMemberPath="Title"
                                                              SelectedValue="{Binding SelectedSourceNodeId, Mode=TwoWay}"/>
                                                    <ComboBox Grid.Column="1"
                                                              Height="32"
                                                              Margin="0,0,8,0"
                                                              Style="{DynamicResource BaseComboBox}"
                                                              ItemsSource="{Binding AvailableOutputKeyOptions}"
                                                              SelectedValuePath="Key"
                                                              DisplayMemberPath="Key"
                                                              SelectedValue="{Binding SelectedSourceOutputKey, Mode=TwoWay}"/>
                                                    <Button Grid.Column="2"
                                                            Content="X"
                                                            Width="28"
                                                            Height="28"
                                                            Style="{DynamicResource DangerButton}"
                                                            Click="RemoveAdditionalAppendSource_Click"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <Button Content="+ Th�m ngu?n append"
                                        Style="{DynamicResource PrimaryButton}"
                                        Height="32"
                                        Click="AddAdditionalAppendSource_Click"
                                        Margin="0,0,0,16"/>
                            </StackPanel>
                            </StackPanel>

                            <TextBlock Text="Output:"
                                       Foreground="{DynamicResource TextBrush}"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,8"/>
                            <Border Background="{DynamicResource WindowBodyBackground}"
                                    BorderBrush="{DynamicResource ControlBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="12"
                                    Margin="0,0,0,16">
                                <StackPanel x:Name="OutputsPanel"/>
                            </Border>

                            <Border Background="{DynamicResource HeaderBackgroundBrush}"
                                    BorderBrush="{DynamicResource PrimaryGlowBrush}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="12">
                                <StackPanel>
                                    <TextBlock Text="?? Runtime"
                                               Foreground="{DynamicResource PrimaryBrush}"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               Margin="0,0,0,8"/>
                                    <TextBlock TextWrapping="Wrap"
                                               Foreground="{DynamicResource TextMuted}"
                                               FontSize="12"
                                               LineHeight="18"
                                               Text="� Kho kv d�ng chung trong m?t l?n ch?y (ExecutionId). Trong Code node: kv['myKey'] d?c; g�n kv['myKey']=x d? ghi (append n?u tr�ng key).&#x0a;� Pass: c?ng IN/OUT tr�n flow d? ghi KV. Get: kh�ng c?n IN; d?c store theo interval.&#x0a;� Cleanup: b?t �Xo� d? li?u� th� lu�n c� c?ng IN d? n?i lu?ng k�ch ho?t x�a."/>
                                </StackPanel>
                            </Border>

                            <Separator Background="{DynamicResource BorderColor}" Margin="0,12,0,12"/>

                            <TextBlock Text="?? KV Data Cleanup"
                                       Foreground="{DynamicResource TextBrush}"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,4"/>
                            <TextBlock TextWrapping="Wrap"
                                       Foreground="{DynamicResource TextSecondary}"
                                       FontSize="11"
                                       LineHeight="16"
                                       Margin="0,0,0,10"
                                       Text="C?u h�nh x�a d? li?u runtime c?a m?t node KeyValueBridge (Pass) kh�c. Lu?ng ph?i ch?y qua c?ng IN c?a node n�y (khi d� b?t �Xo� d? li?u�) th� cleanup m?i ch?y theo workflow. N�t �Th?c hi?n x�a� ch? d? th? tay tr�n m�y b?n."/>

                            <StackPanel Visibility="{Binding EnableDataCleanup, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock Text="1) �i?u ki?n trigger (b?t bu?c)"
                                           Foreground="{DynamicResource TextSecondary}"
                                           FontSize="12"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,4"/>
                                <TextBlock TextWrapping="Wrap"
                                           Text="Ch?n node c� output d?ng, r?i ch?n output key. C?t ph?i: ch?n gi� tr? k? v?ng. V?i k? v?ng �true� ho?c �1�, gi� tr? trigger th?c t? du?c coi l� kh?p n?u l� truthy (true, 1, yes, s? kh�c 0�). V?i �false�/�0� th� c?n falsy (false, 0, no�). Kh�ng kh?p th� b? qua x�a."
                                           Foreground="{DynamicResource TextSecondary}"
                                           FontSize="11"
                                           LineHeight="16"
                                           Margin="0,0,0,6"/>
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <ComboBox Grid.Column="0"
                                              Height="36"
                                              Margin="0,0,6,0"
                                              Style="{DynamicResource BaseComboBox}"
                                              ToolTip="Node ngu?n: ph?i c� �t nh?t m?t output d?ng (dynamic output)."
                                              ItemsSource="{Binding CleanupTriggerSourceOptions}"
                                              SelectedValuePath="NodeId"
                                              DisplayMemberPath="Title"
                                              SelectedValue="{Binding CleanupTriggerSourceNodeId, Mode=TwoWay}"/>
                                    <ComboBox Grid.Column="1"
                                              Height="36"
                                              Margin="0,0,6,0"
                                              Style="{DynamicResource BaseComboBox}"
                                              ToolTip="Output key tr�n node trigger � gi� tr? s? du?c so v?i � b�n ph?i."
                                              ItemsSource="{Binding CleanupTriggerOutputKeyOptions}"
                                              SelectedValuePath="Key"
                                              DisplayMemberPath="DisplayName"
                                              SelectedValue="{Binding CleanupTriggerSourceOutputKey, Mode=TwoWay}"/>
                                    <ComboBox Grid.Column="2"
                                              Height="36"
                                              Style="{DynamicResource BaseComboBox}"
                                              ToolTip="Gi� tr? k? v?ng (so kh?p ki?u boolean/s?)."
                                              ItemsSource="{Binding CleanupTriggerExpectedValueOptions}"
                                              SelectedItem="{Binding CleanupTriggerExpectedValue, Mode=TwoWay}"/>
                                </Grid>
                                <TextBlock Text="C?t tr�i ? gi?a ? ph?i: node trigger � output d?c gi� tr? � gi� tr? k? v?ng."
                                           Foreground="{DynamicResource TextMuted}"
                                           FontSize="10"
                                           Margin="0,0,0,6"/>

                                <CheckBox Content="Khi trigger kh?p: x�a to�n b? KV runtime c?a node d�ch (b? qua key / filter b�n du?i)"
                                          Foreground="{DynamicResource DangerColor}"
                                          FontSize="12"
                                          IsChecked="{Binding ClearAllDataForSelectedNode, Mode=TwoWay}"
                                          Margin="0,0,0,4"/>
                                <TextBlock TextWrapping="Wrap"
                                           Text="Tick: sau khi di?u ki?n trigger th?a, x�a h?t key c?a node KeyValueBridge d�ch d� ch?n ? bu?c 2. B? tick: x�a theo key / l?c m?ng ? c�c bu?c du?i."
                                           Foreground="{DynamicResource TextSecondary}"
                                           FontSize="11"
                                           LineHeight="16"
                                           Margin="22,0,0,10"/>

                                <TextBlock Text="2) Node KV d�ch (Pass) b? x�a data"
                                           Foreground="{DynamicResource TextSecondary}"
                                           FontSize="12"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,4"/>
                                <TextBlock TextWrapping="Wrap"
                                           Text="Li?t k� c�c KeyValueBridge Pass ho?c Get (kh�ng b?t �Xo� d? li?u� tr�n ch�nh node d�). Ch?n Pass d? x�a tr?c ti?p kho kv g?c; ch?n Get th� x�a c? scope Get v� Pass ngu?n (n?u c�). Node cleanup hi?n t?i kh�ng n?m trong danh s�ch."
                                           Foreground="{DynamicResource TextSecondary}"
                                           FontSize="11"
                                           LineHeight="16"
                                           Margin="0,0,0,6"/>
                                <ComboBox Height="36"
                                          Style="{DynamicResource BaseComboBox}"
                                          Margin="0,0,0,8"
                                          ToolTip="Node KVB Pass ho?c Get (kh�ng ph?i node cleanup). Pass = x�a kho kv g?c; Get = x�a kv Get + kv Pass ngu?n n?u c�."
                                          ItemsSource="{Binding DeleteBridgeNodeOptions}"
                                          SelectedValuePath="NodeId"
                                          DisplayMemberPath="Title"
                                          SelectedValue="{Binding SelectedDeleteBridgeNodeId, Mode=TwoWay}"/>

                                <StackPanel Visibility="{Binding ClearAllDataForSelectedNode, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                <TextBlock Text="3) Key c?n x�a (ho?c x�a c? node)"
                                           Foreground="{DynamicResource TextSecondary}"
                                           FontSize="12"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,4"/>
                                <TextBlock TextWrapping="Wrap"
                                           Text="Ch? hi?n khi kh�ng b?t �x�a to�n b?� ? tr�n: nh?p ho?c ch?n m?t key trong � combobox (danh s�ch t? snapshot runtime). Hai combobox map d?ng l� t�y ch?n."
                                           Foreground="{DynamicResource TextSecondary}"
                                           FontSize="11"
                                           LineHeight="16"
                                           Margin="0,0,0,6"/>
                                <TextBlock Text="Map d?ng t�n key (t�y ch?n):"
                                           Foreground="{DynamicResource TextSecondary}"
                                           FontSize="11"
                                           Margin="0,0,0,4"/>
                                <Grid Margin="0,0,0,6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <ComboBox Grid.Column="0"
                                              Height="36"
                                              Margin="0,0,4,0"
                                              Style="{DynamicResource BaseComboBox}"
                                              ToolTip="Node c� output string � thu?ng l� t�n key c?n x�a do lu?ng tru?c t�nh ra."
                                              ItemsSource="{Binding CleanupKeySourceOptions}"
                                              SelectedValuePath="NodeId"
                                              DisplayMemberPath="Title"
                                              SelectedValue="{Binding CleanupKeySourceNodeId, Mode=TwoWay}"/>
                                    <ComboBox Grid.Column="1"
                                              Height="36"
                                              Margin="4,0,0,0"
                                              Style="{DynamicResource BaseComboBox}"
                                              ToolTip="Output key ch?a chu?i t�n key."
                                              ItemsSource="{Binding CleanupKeySourceOutputKeyOptions}"
                                              SelectedValuePath="Key"
                                              DisplayMemberPath="DisplayName"
                                              SelectedValue="{Binding CleanupKeySourceOutputKey, Mode=TwoWay}"/>
                                </Grid>
                                <ComboBox Height="36"
                                          Style="{DynamicResource BaseComboBox}"
                                          Margin="0,0,0,8"
                                          IsEditable="True"
                                          ToolTip="Key tinh (g�) ho?c ch?n t? danh s�ch key dang c� tr�n node d�ch. N?u d� map d?ng ? tr�n th� map du?c uu ti�n khi ch?y."
                                          ItemsSource="{Binding DeleteKeyOptions}"
                                          Text="{Binding DeleteTargetKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                                <TextBlock Text="4) X�a theo ph?n t? trong m?ng JSON (t�y ch?n)"
                                           Foreground="{DynamicResource TextSecondary}"
                                           FontSize="12"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,4"/>
                                <TextBlock TextWrapping="Wrap"
                                           Text="D�ng khi value c?a key l� m?ng object v� b?n ch? mu?n b? c�c ph?n t? c� field = value. C� th? g� tinh ? hai � text, ho?c map th�m t? output node (uu ti�n khi ch?y n?u d� bind)."
                                           Foreground="{DynamicResource TextSecondary}"
                                           FontSize="11"
                                           LineHeight="16"
                                           Margin="0,0,0,6"/>
                                <TextBlock Text="T�n field JSON (map d?ng t�y ch?n):"
                                           Foreground="{DynamicResource TextSecondary}"
                                           FontSize="11"
                                           Margin="0,0,0,4"/>
                                <Grid Margin="0,0,0,6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <ComboBox Grid.Column="0"
                                              Height="36"
                                              Margin="0,0,4,0"
                                              Style="{DynamicResource BaseComboBox}"
                                              ToolTip="Node cung c?p t�n field (string), v� d? t�n c?t trong object m?ng."
                                              ItemsSource="{Binding CleanupFilterFieldSourceOptions}"
                                              SelectedValuePath="NodeId"
                                              DisplayMemberPath="Title"
                                              SelectedValue="{Binding CleanupFilterFieldSourceNodeId, Mode=TwoWay}"/>
                                    <ComboBox Grid.Column="1"
                                              Height="36"
                                              Margin="4,0,0,0"
                                              Style="{DynamicResource BaseComboBox}"
                                              ToolTip="Output key ? chu?i t�n field."
                                              ItemsSource="{Binding CleanupFilterFieldSourceOutputKeyOptions}"
                                              SelectedValuePath="Key"
                                              DisplayMemberPath="DisplayName"
                                              SelectedValue="{Binding CleanupFilterFieldSourceOutputKey, Mode=TwoWay}"/>
                                </Grid>
                                <TextBlock Text="Gi� tr? tinh: t�n field (tr�i) � gi� tr? c?n kh?p (ph?i)"
                                           Foreground="{DynamicResource TextSecondary}"
                                           FontSize="11"
                                           Margin="0,0,0,4"/>
                                <Grid Margin="0,0,0,6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0"
                                             Height="36"
                                             Margin="0,0,4,0"
                                             Style="{DynamicResource BaseTextBoxV2}"
                                             ToolTip="V� d?: cardId � tru?ng trong m?i object c?a m?ng."
                                             Text="{Binding ArrayFilterField, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                    <TextBox Grid.Column="1"
                                             Height="36"
                                             Margin="4,0,0,0"
                                             Style="{DynamicResource BaseTextBoxV2}"
                                             ToolTip="V� d?: UUID ho?c chu?i id � object n�o c� field b?ng gi� tr? n�y th� b? lo?i kh?i m?ng."
                                             Text="{Binding ArrayFilterValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </Grid>
                                <TextBlock Text="V� d?: field = cardId, value = db90733a-aed0-440a-ba1d-6773271268b9"
                                           Foreground="{DynamicResource TextMuted}"
                                           FontSize="10"
                                           Margin="0,0,0,6"/>
                                <TextBlock Text="Gi� tr? filter � map d?ng (t�y ch?n, thay cho � text ph?i):"
                                           Foreground="{DynamicResource TextSecondary}"
                                           FontSize="11"
                                           Margin="0,0,0,4"/>
                                <Grid Margin="0,0,0,6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <ComboBox Grid.Column="0"
                                              Height="36"
                                              Margin="0,0,4,0"
                                              Style="{DynamicResource BaseComboBox}"
                                              ToolTip="Node ngu?n gi� tr? c?n so kh?p (c�ng nghia v?i � text ph?i)."
                                              ItemsSource="{Binding CleanupFilterValueSourceOptions}"
                                              SelectedValuePath="NodeId"
                                              DisplayMemberPath="Title"
                                              SelectedValue="{Binding CleanupFilterValueSourceNodeId, Mode=TwoWay}"/>
                                    <ComboBox Grid.Column="1"
                                              Height="36"
                                              Margin="4,0,0,0"
                                              Style="{DynamicResource BaseComboBox}"
                                              ToolTip="Output key ? gi� tr? so kh?p v?i field d� ch?n."
                                              ItemsSource="{Binding CleanupFilterValueSourceOutputKeyOptions}"
                                              SelectedValuePath="Key"
                                              DisplayMemberPath="DisplayName"
                                              SelectedValue="{Binding CleanupFilterValueSourceOutputKey, Mode=TwoWay}"/>
                                </Grid>
                                <CheckBox Content="X�a t?t c? item kh?p di?u ki?n (t?t = ch? x�a 1 item)"
                                          Foreground="{DynamicResource TextSecondary}"
                                          FontSize="12"
                                          IsChecked="{Binding RemoveAllMatchedArrayItems, Mode=TwoWay}"
                                          Margin="0,0,0,8"/>
                                <Border Background="{DynamicResource CardColor}"
                                        BorderBrush="{DynamicResource BorderColor}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Padding="8"
                                        Margin="0,0,0,8">
                                    <StackPanel>
                                        <TextBlock Text="Effective key / filter (debug):"
                                                   Foreground="{DynamicResource PrimaryBrush}"
                                                   FontSize="11"
                                                   Margin="0,0,0,4"/>
                                        <TextBlock Text="{Binding EffectiveDeleteKeyPreview}"
                                                   Foreground="{DynamicResource TextBrush}"
                                                   FontSize="11"
                                                   TextWrapping="Wrap"
                                                   Margin="0,0,0,2"/>
                                        <TextBlock Text="{Binding EffectiveFilterValuePreview}"
                                                   Foreground="{DynamicResource TextBrush}"
                                                   FontSize="11"
                                                   TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>

                                <Button Content="Th?c hi?n x�a KV data"
                                        Height="32"
                                        Style="{DynamicResource DangerButton}"
                                        Cursor="Hand"
                                        Margin="0,0,0,8"
                                        Command="{Binding DeleteKvDataCommand}"/>

                                <Border Background="{DynamicResource CardColor}"
                                        CornerRadius="6"
                                        Padding="10,8"
                                        Margin="0,0,0,8">
                                    <TextBlock Text="{Binding DeleteResultMessage}"
                                               Foreground="{DynamicResource TextBrush}"
                                               FontSize="12"
                                               TextWrapping="Wrap"/>
                                </Border>
                                </StackPanel>

                                <TextBlock Text="Preview d? li?u hi?n t?i tru?c khi x�a:"
                                           Foreground="{DynamicResource TextSecondary}"
                                           FontSize="12"
                                           Margin="0,0,0,6"/>
                                <Button Content="Refresh Preview"
                                        Height="28"
                                        Width="130"
                                        HorizontalAlignment="Left"
                                        Margin="0,0,0,6"
                                        Style="{DynamicResource PrimaryButton}"
                                        Cursor="Hand"
                                        Command="{Binding RefreshDeletePreviewNowCommand}"/>
                                <Border Background="{DynamicResource WindowBackground}"
                                        BorderBrush="{DynamicResource BorderColor}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Padding="8"
                                        Margin="0,0,0,0">
                                    <ScrollViewer MaxHeight="180" VerticalScrollBarVisibility="Auto">
                                        <TextBlock Text="{Binding DeletePreviewJson}"
                                                   Foreground="{DynamicResource TextBrush}"
                                                   FontSize="11"
                                                   FontFamily="Consolas"
                                                   TextWrapping="Wrap"/>
                                    </ScrollViewer>
                                </Border>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="T�i s? d?ng flow" Style="{StaticResource HttpTabItemStyle}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
                        <StackPanel>
                            <TextBlock Text="V? tr� c?ng IN/OUT:"
                                       Foreground="{DynamicResource TextBrush}"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,8"/>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                    <TextBlock Text="Port IN"
                                               Foreground="{DynamicResource TextBrush}"
                                               FontSize="12"
                                               Opacity="0.8"
                                               Margin="0,0,0,4"/>
                                    <ComboBox Height="32"
                                              Style="{DynamicResource BaseComboBox}"
                                              ItemsSource="{Binding PortPositionOptions}"
                                              SelectedItem="{Binding InputPortPosition, Mode=TwoWay}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                    <TextBlock Text="Port OUT"
                                               Foreground="{DynamicResource TextBrush}"
                                               FontSize="12"
                                               Opacity="0.8"
                                               Margin="0,0,0,4"/>
                                    <ComboBox Height="32"
                                              Style="{DynamicResource BaseComboBox}"
                                              ItemsSource="{Binding PortPositionOptions}"
                                              SelectedItem="{Binding OutputPortPosition, Mode=TwoWay}"/>
                                </StackPanel>
                            </Grid>
                            <ItemsControl ItemsSource="{Binding ReuseRoutes}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,0,0,12"
                                                Background="{DynamicResource WindowBackground}"
                                                BorderBrush="{DynamicResource ControlBorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="8"
                                                Padding="10">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="2*" />
                                                    <ColumnDefinition Width="2*" />
                                                    <ColumnDefinition Width="2*" />
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="Node IN"
                                                               Foreground="{DynamicResource TextBrush}"
                                                               FontSize="12"
                                                               Opacity="0.7"
                                                               Margin="0,0,0,4"/>
                                                    <TextBlock Text="{Binding IncomingNodeTitle}"
                                                               Foreground="{DynamicResource TextBrush}"
                                                               FontSize="13"
                                                               FontWeight="SemiBold"
                                                               TextTrimming="CharacterEllipsis"/>
                                                </StackPanel>
                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="Node OUT"
                                                               Foreground="{DynamicResource TextBrush}"
                                                               FontSize="12"
                                                               Opacity="0.7"
                                                               Margin="0,0,0,4"/>
                                                    <ComboBox Height="32"
                                                              Style="{DynamicResource BaseComboBox}"
                                                              ItemsSource="{Binding OutgoingOptions}"
                                                              SelectedValuePath="NodeId"
                                                              DisplayMemberPath="Title"
                                                              SelectedValue="{Binding SelectedOutgoingNodeId, Mode=TwoWay}"/>
                                                </StackPanel>
                                                <StackPanel Grid.Column="2">
                                                    <TextBlock Text="Ki?u line OUT"
                                                               Foreground="{DynamicResource TextBrush}"
                                                               FontSize="12"
                                                               Opacity="0.7"
                                                               Margin="0,0,0,4"/>
                                                    <ComboBox Height="32"
                                                              Style="{DynamicResource BaseComboBox}"
                                                              ItemsSource="{Binding DataContext.ConnectionLineStyleOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                                                              SelectedValuePath="Key"
                                                              DisplayMemberPath="DisplayName"
                                                              SelectedValue="{Binding SelectedLineStyleKey, Mode=TwoWay}"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
            </TabControl>
        </Grid>
    </Border>
</local:BaseNodeDialog>
