<UserControl x:Class="FlowMy.Controls.MarkdownTextBlock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400">
    <Grid>
        <!-- RichTextBox để hiển thị markdown đã render -->
        <RichTextBox x:Name="MarkdownContent"
                     IsReadOnly="True"
                     BorderThickness="0"
                     Background="Transparent"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled"
                     Visibility="Collapsed"
                     MinHeight="20">
            <RichTextBox.Resources>
                <Style TargetType="Paragraph">
                    <Setter Property="Margin" Value="0,4,0,4"/>
                </Style>
            </RichTextBox.Resources>
        </RichTextBox>
        
        <!-- TextBlock để hiển thị text thường nếu không phải markdown -->
        <TextBlock x:Name="PlainTextContent"
                   TextWrapping="Wrap"
                   Visibility="Collapsed"
                   MinHeight="20"/>
    </Grid>
</UserControl>

